# ============================================================================
# DÉPENDANCES DU PIPELINE AMÉLIORÉ
# ============================================================================
# Installation: pip install -r requirements_improved.txt

# === Core Data Science ===
numpy>=1.24.0,<3.0.0
pandas>=2.0.0,<3.0.0
scipy>=1.10.0,<2.0.0

# === Machine Learning ===
scikit-learn>=1.3.0,<2.0.0
torch>=2.0.0,<3.0.0
transformers>=4.30.0,<5.0.0

# === NLP et Embeddings ===
sentence-transformers>=2.2.0,<4.0.0
rank-bm25>=0.2.2,<0.3.0
langdetect>=1.0.9,<2.0.0
ftfy>=6.1.0,<7.0.0
beautifulsoup4>=4.12.0,<5.0.0

# === PDF Processing ===
PyMuPDF>=1.23.0,<2.0.0  # Extraction de texte des PDFs scientifiques

# === Clustering Avancé ===
# HDBSCAN: clustering hiérarchique robuste (recommandé)
# Installation optionnelle mais fortement conseillée
# hdbscan>=0.8.33,<1.0.0

# === Visualisation ===
# Pour les graphiques et visualisations (REQUIS pour generate_visualizations.py)
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# === Analyse Statistique ===
# Tests statistiques et analyse de sensibilité
statsmodels>=0.14.0,<1.0.0

# SALib pour analyse de sensibilité (optionnel mais utile)
# Décommentez si vous voulez l'analyse de Sobol
# SALib>=1.4.0,<2.0.0

# === Optimisation Submodulaire (optionnel) ===
# Pour Facility Location optimale
# Décommentez si vous voulez des garanties théoriques d'approximation
# submodlib>=2.0.0,<3.0.0

# === Sécurité ===
# Sanitisation HTML avancée
# Décommentez si vous traitez du contenu web non fiable
# bleach>=6.0.0,<7.0.0
# html-sanitizer>=2.0.0,<3.0.0

# === Performance ===
# Accélération avec FAISS (pour très gros corpus)
# Décommentez pour activer la recherche de similarité rapide
# faiss-cpu>=1.7.4,<2.0.0  # Version CPU
# OU
# faiss-gpu>=1.7.4,<2.0.0  # Version GPU (nécessite CUDA)

# === Logging et Monitoring ===
# Structlog pour logging structuré (optionnel)
# structlog>=23.0.0,<24.0.0

# === Tests et Qualité de Code ===
pytest>=7.4.0,<9.0.0
pytest-cov>=4.1.0,<6.0.0

# === Linting et Formatage (optionnel mais recommandé) ===
# black>=23.0.0,<25.0.0
# ruff>=0.1.0,<1.0.0
# mypy>=1.5.0,<2.0.0

# ============================================================================
# NOTES D'INSTALLATION
# ============================================================================

# === Installation de base (minimal) ===
# pip install numpy pandas scipy scikit-learn torch sentence-transformers rank-bm25 langdetect ftfy

# === Installation recommandée (avec HDBSCAN) ===
# pip install -r requirements_improved.txt

# === Installation complète (toutes les options) ===
# Décommentez toutes les dépendances optionnelles ci-dessus

# === Résolution de problèmes ===

# 1. Si erreur avec torch sur Windows:
#    pip install torch --index-url https://download.pytorch.org/whl/cpu

# 2. Si erreur avec hdbscan:
#    pip install --upgrade setuptools wheel
#    pip install hdbscan

# 3. Pour GPU (CUDA):
#    pip install torch --index-url https://download.pytorch.org/whl/cu118
#    (remplacez cu118 par votre version CUDA)

# 4. Pour environnement isolé (recommandé):
#    python -m venv venv
#    venv\Scripts\activate  # Windows
#    source venv/bin/activate  # Linux/Mac
#    pip install -r requirements_improved.txt

# ============================================================================
# COMPATIBILITÉ
# ============================================================================
# Python: >= 3.8, < 3.12
# OS: Windows 10+, Linux, macOS
# RAM: Minimum 8 GB, Recommandé 16 GB pour gros corpus
# GPU: Optionnel (accélère les embeddings)

